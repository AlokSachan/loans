server:
  port: 8090
spring:
  application:
    name: "loans"
  datasource:
    url: jdbc:mysql://localhost:3307/loansdb
    username: root
    password: alok
  jpa:
    show-sql: true

  config:
    import: "optional:configserver:http://localhost:8071/"
  sql:
    init:
      mode: always
#  rabbitmq:
#    host: "localhost"
#    username: "guest"
#    password: "guest"
#    port: 5672

management:
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    env:
      enabled: true
  endpoint:
    shutdown:
      enabled: true
endpoints:
  shutdown:
    enabled: true
eureka:
  instance:
    hostname: localhost
  client:
    serviceUrl:
      defaultZone: http://localhost:8070/eureka/
    fetchRegistry: true
    registerWithEureka: true
info:
  app:
    name: "loans"
    description: "ABC Bank Loans Application"
    version: 1.0.0
logging:
  level:
    com:
      alok:
        loans: DEBUG
